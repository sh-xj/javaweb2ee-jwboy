plugins {
    id 'war'  
	id 'org.gretty' version '2.2.0' 
}

repositories {
    // flatDir { dirs "../gradlelibs" }
    // flatDir {  dirs 'lib'  }
     //flatDir {  dirs 'lib1', 'lib2' }
    jcenter()
	//mavenLocal()
    //mavenCentral()
}

dependencies {
    providedCompile 'javax.servlet:javax.servlet-api:3.1.0' 
}

tasks.withType(JavaCompile) {  
    options.encoding = "UTF-8"  
}
